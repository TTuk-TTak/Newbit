<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.newbit.model.mapper.GraphMapper">


	<select id="getKeyword" parameterType="int" resultType="String">
		select c.content_keyword
		from content c
		where (c.content_code in (select s.content_code from contentscrap s where
		s.user_code = #{uid}))
		or (c.content_code in ( select p.content_code from post p where p.user_code =
		#{uid}));
	</select>

</mapper>